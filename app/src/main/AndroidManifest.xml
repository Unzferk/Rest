<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".menuactivities.DishDetails"></activity>
        <activity android:name=".kitchenActivities.KitchenOrderDetail" />
        <activity android:name=".kitchenActivities.Kitchen" />
        <activity android:name=".orderactivities.OrderDetails" />
        <activity android:name=".orderactivities.Order" />
        <activity
            android:name=".orderactivities.OrderQueue"
            android:label="@string/title_activity_order_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".menuactivities.AddDish" />
        <activity
            android:name=".menuactivities.Menu"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>